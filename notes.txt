A faire :

- sidebar eeg : channels enlevés + logs


- etape filter : ajouter choix pour faire apply notch; enlever Resample FReq (Hz) 
- etape resampling : params = Resample FReq (Hz) (pas moins que 2x le lowpass du Filter
- etape bad channels : l'user choisit avec le plot les bads channels
- etape interpolation : pas de param
- etape referencing : pas de param (average), 
- etape trimming : param = input pour la durée d'enregistrement jusqu'apres la fin du dernier trigger. Il faut créer une function qui enlève le debut des signaux (avant le premier trigger) et la fin des signaux (après le dernier trigger + la durée en input)  


Run ICA dans une autre tab
- etape ICA : . Enlever random state, enlever 

Run Epoching dans une autre tab

----

etape  : interpoler vref

data.set_channel_types({'E125':'misc','E126':'misc','E127':'misc','E128':'misc', 'E119':'misc', 'E48':'misc'}) #TODO tcheck if those chan are EEG or not (mentoniere) ?
    data.info['bads'].extend(['E129']) # + ['Cz']
